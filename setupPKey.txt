Windows: "%JAVA_HOME%\bin\keytool" -genkey -alias tomcat -keyalg RSA

Unix: $JAVA_HOME/bin/keytool -genkey -alias tomcat -keyalg RSA

password is:
	changeit

"Your first and last name" must be the host that the connection is going through
So if you'll be testing it on localhost you enter "localhost"
If you're testing it on your ip you put your ip
otherwise it'll see that the "common name" doesn't match

Then you have to configure your browser to accept the certificate

This basically isn't possible in chrome

In firefox when you go to the "not trusted" page just hit "add exception"